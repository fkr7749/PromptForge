{{- if .Values.safetyService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "promptforge.fullname" . }}-safety-service
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "promptforge.labels" . | nindent 4 }}
    app.kubernetes.io/component: safety-service
spec:
  replicas: {{ .Values.safetyService.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/component: safety-service
      {{- include "promptforge.selectorLabels" . | nindent 6 }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/component: safety-service
        {{- include "promptforge.selectorLabels" . | nindent 8 }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "4009"
        prometheus.io/path: "/metrics"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: safety-service
          image: "{{ .Values.global.imageRegistry }}/{{ .Values.global.imageOwner }}/{{ .Values.safetyService.image.name }}:{{ .Values.global.imageTag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: 4009
              protocol: TCP
          envFrom:
            - configMapRef:
                name: promptforge-config
            - secretRef:
                name: promptforge-secrets
          resources:
            {{- toYaml .Values.safetyService.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: 4009
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 4009
            initialDelaySeconds: 15
            periodSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health
              port: 4009
            failureThreshold: 30
            periodSeconds: 10
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app.kubernetes.io/component
                      operator: In
                      values: [safety-service]
                topologyKey: kubernetes.io/hostname
{{- end }}
