{{- if .Values.searchService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "promptforge.fullname" . }}-search-service
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "promptforge.labels" . | nindent 4 }}
    app.kubernetes.io/component: search-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: search-service
    {{- include "promptforge.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.searchService.service.port }}
      targetPort: http
      protocol: TCP
{{- end }}
