---
# ============================================================
# Role: k3s_control — Handlers
# ============================================================

- name: restart k3s
  ansible.builtin.systemd:
    name: "{{ k3s_systemd_service }}"
    state: restarted
    daemon_reload: true
  become: true
