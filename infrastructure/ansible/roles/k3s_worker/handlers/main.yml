---
# ============================================================
# Role: k3s_worker — Handlers
# ============================================================

- name: restart k3s-agent
  ansible.builtin.systemd:
    name: "{{ k3s_systemd_service }}"
    state: restarted
    daemon_reload: true
  become: true
